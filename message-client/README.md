# Client app

A demo app to interact with the message-api service

## Install

After cloning the **kubernetest** repository, go inside the **message-client** directory and run `npm install` in order to install dependecies.

`npm run serve` : starts a development server

`npm run build` : builds app for production
